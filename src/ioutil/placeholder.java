package ioutil;

public class placeholder {
	// note that abstract class can be used in place of a singleton. We don't need a singleton process, just abstract.
	public void runner() {
		SpreadSheetHelper.readPoints(null);
	}
}
